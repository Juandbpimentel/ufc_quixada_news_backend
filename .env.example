DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ufcnews?schema=public"
JWT_SECRET="change-me"
PORT=8080
PUBLIC_API_URL="http://localhost:8080" # alterar para a URL pública quando for para produção

# CORS (se houver frontend)
FRONTEND_URL="http://localhost:3000"
ALLOWED_ORIGINS="http://localhost:3000"

# Seed do admin inicial
ADMIN_LOGIN="adminufcnews"
ADMIN_PASSWORD="adminpass"
ADMIN_EMAIL="admin@ufcnews.com"
ADMIN_NAME="Admin"

MEUCONTATO_URL="https://meucontato-backend.onrender.com"
PASSWORD_RESET_URL="https://app.exemplo.com/auth/verify"
PASSWORD_RESET_EXPIRES_MINUTES=60
MAIL_FROM_NAME="UFC News"
MAIL_FROM_EMAIL="no-reply@ufcnews.com"
PASSWORD_RESET_SUBJECT="Redefinição de senha"
# docker-compose
POSTGRES_DB=ufcnews
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY_B64=
FIREBASE_STORAGE_BUCKET=

# Optional: limits for uploads / JSON body (useful if your frontend sends base64 images)
# Default JSON body limit (in MB): 200
JSON_BODY_LIMIT_MB=200
# Maximum allowed image upload size (in MB) enforced by the server (default: 200)
UPLOAD_MAX_MB=200